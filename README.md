# oss-revenue-calc
A CLI tool that calculates how much revenue AI coding platforms would theoretically owe open source maintainers under the proposed 'Spotify model' for AI-generated package usage. Given a package name, estimated AI-driven download share, and platform subscription pricing inputs, it fetches real download statistics from PyPI/npm, runs configurable re
